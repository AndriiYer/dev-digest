# Insights — e2e

Nothing recorded yet. Add entries here when something about this package's
behavior surprises you and isn't obvious from the code (a workaround, a
non-obvious constraint, a decision that would otherwise get re-litigated).
