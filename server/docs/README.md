# server/docs

Deep-dive docs for `@devdigest/api` (architecture decisions, module design
notes) that are too long-lived or detailed for `server/README.md`.

Empty for now — add docs here as they're written.
