# server/specs

Specs/contracts owned by `@devdigest/api` (route contracts, module specs)
that aren't already captured by the Zod schemas in
[src/vendor/shared](../src/vendor/shared).

Empty for now — add specs here as they're written.
